[core]
1password = "cask"  # Password manager that keeps all passwords secure behind one password
1password-cli = "cask"  # Command-line interface for 1Password [command: op]
mas = "formula"  # Mac App Store command-line interface
uv = "formula"  # Extremely fast Python package installer and resolver, written in Rust

[developer]
497799835 = "mas"  # Xcode
charles = "cask"  # Web debugging Proxy application
chatgpt = "cask"  # OpenAI's official ChatGPT desktop app
"claude-code@latest" = "cask"  # Terminal-based AI coding assistant
codex = "cask"  # OpenAI's coding agent that runs in your terminal
cursor = "cask"  # Write, edit, and chat about your code with AI
cursor-cli = "cask"  # Command-line agent for Cursor
docker-desktop = "cask"  # App to build and share containerised applications and microservices
gcloud-cli = "cask"  # Set of tools to manage resources and applications hosted on Google Cloud
ghostty = "cask"  # Terminal emulator that uses platform-native UI and GPU acceleration
icon-composer = "cask"  # Apple tool to create multi-platform icons
"postgresql@15" = "formula"  # Object-relational database system
redis = "formula"  # Persistent key-value database, with built-in net interface
xcodegen = "formula"  # Generate your Xcode project from a spec file and your folder structure

[work]
aws-sso-cli = "formula"  # Securely manage AWS API credentials using AWS SSO
awscli = "formula"  # Official Amazon AWS command-line interface
citrix-workspace = "cask"  # Managed desktop virtualization solution
microsoft-excel = "cask"  # Spreadsheet software
microsoft-teams = "cask"  # Meet, chat, call, and collaborate in just one place
microsoft-word = "cask"  # Word processor
powershell = "formula"  # Command-line shell and scripting language
windows-app = "cask"  # Connect to Windows

[productivity]
granola = "cask"  # AI-powered notepad for meetings
linear = "cask"  # App to manage software development and track bugs
notion = "cask"  # App to write, plan, collaborate, and get organised
obsidian = "cask"  # Knowledge base that works on top of a local folder of plain text Markdown files
slack = "cask"  # Team communication and collaboration software
todoist-app = "cask"  # To-do list
zoom = "cask"  # Video communication and virtual meeting platform

[leisure]
302584613 = "mas"  # Amazon Kindle: Read eBooks & Magazines
"martimlobao/dos-games/jumpstart-1st-grade" = "cask"  # Windows first grade educational game from 1995
"martimlobao/dos-games/jumpstart-kindergarten" = "cask"  # Windows Kindergarten educational game from 1994
"martimlobao/dos-games/mission-think" = "cask"  # Windows educational game from 1997
"martimlobao/dos-games/ods-vol1" = "cask"  # Educational compilation by Ohio Distinctive Software from 1997
"martimlobao/dos-games/orlys-draw-a-story" = "cask"  # Windows drawing and storytelling game by Broderbund from 1997
"martimlobao/dos-games/ready-to-read-with-pooh" = "cask"  # Windows reading game by Disney from 1997
minecraft = "cask"  # Sandbox construction video game
spotify = "cask"  # Music streaming service

[social]
telegram = "cask"  # Messaging app with a focus on speed and security
whatsapp = "cask"  # Native desktop client for WhatsApp

[fonts]
font-chomsky = "cask"  # Chomsky (a font in the style of the New York Times masthead)
font-doto = "cask"  # Doto
font-eb-garamond = "cask"  # EB Garamond
font-fira-code = "cask"  # Fira Code
font-instrument-serif = "cask"  # Instrument Serif
font-new-york = "cask"  # New York, NY
font-pixel-code = "cask"  # Pixel Code
font-sauce-code-pro-nerd-font = "cask"  # SauceCodePro Nerd Font (Source Code Pro)
font-sf-mono = "cask"  # San Francisco Mono, SF Mono
font-sf-pro = "cask"  # San Francisco Pro, SF Pro
font-source-code-pro = "cask"  # Source Code Pro
font-source-sans-3 = "cask"  # Source Sans 3
font-with-a-syntax-highlighter = "cask"  # Font With A Syntax Highlighter
"martimlobao/fonts/font-neacademia" = "cask"  # Neacademia
"martimlobao/nyt-fonts/font-cheltenham" = "cask"  # Cheltenham NYT font family
"martimlobao/nyt-fonts/font-franklin" = "cask"  # Franklin NYT font family
"martimlobao/nyt-fonts/font-imperial" = "cask"  # Imperial NYT font family
"martimlobao/nyt-fonts/font-karnak" = "cask"  # Karnak NYT font family
"martimlobao/nyt-fonts/font-mag" = "cask"  # NYT Magazine font family
"martimlobao/nyt-fonts/font-schnyder" = "cask"  # Schnyder NYT font family
"martimlobao/nyt-fonts/font-stymie" = "cask"  # Stymie NYT font family

[tools]
1193539993 = "mas"  # Brother iPrint&Scan
937984704 = "mas"  # Amphetamine: Powerful keep-awake utility
blip = "cask"  # Send any size file between devices
calibre = "cask"  # E-books management software
chatgpt-atlas = "cask"  # OpenAI's official browser with ChatGPT built in
claude = "cask"  # Anthropic's official Claude AI desktop app
dropbox = "cask"  # Client for the Dropbox cloud storage service
google-drive = "cask"  # Client for the Google Drive storage service
iina = "cask"  # Free and open-source media player [modern vlc alternative]
istat-menus = "cask"  # System monitoring app
localsend = "cask"  # Open-source cross-platform alternative to AirDrop
nordvpn = "cask"  # VPN client for secure internet access and private browsing
openvpn-connect = "cask"  # Client program for the OpenVPN Access Server
rocket = "cask"  # Emoji picker optimised for blind people
tailscale-app = "cask"  # Mesh VPN based on WireGuard
the-unarchiver = "cask"  # Unpacks archive files
thebrowsercompany-dia = "cask"  # Web browser
transmission = "cask"  # Open-source BitTorrent client
utm = "cask"  # Virtual machines UI using QEMU
whatcable = "cask"  # Menu bar app for USB-C cable diagnostics
wispr-flow = "cask"  # Voice-to-text dictation with AI-powered auto-editing
yubico-authenticator = "cask"  # Application for generating TOTP and HOTP codes

[cli-tools]
ack = "formula"  # Search tool like grep, but optimized for programmers
actionlint-py = "uv"  # Static checker for GitHub Actions workflow files
basictex = "cask"  # Compact TeX distribution as alternative to the full TeX Live / MacTeX
bat = "formula"  # Clone of cat(1) with syntax highlighting and Git integration
bun = "formula"  # Incredibly fast JavaScript runtime, bundler, test runner, and package manager
checkov = "uv"  # Prevent cloud misconfigurations during build-time for IaC tools
domain-check = "formula"  # CLI tool for checking domain availability using RDAP and WHOIS protocols
exiftool = "formula"  # Perl lib for reading and writing EXIF metadata
eza = "formula"  # Modern, maintained replacement for ls
ffmpeg = "formula"  # Play, record, convert, and stream audio and video
fresh-editor = "formula"  # Text editor for your terminal: easy, powerful and fast
gh = "formula"  # GitHub command-line tool
ghostscript = "formula"  # Interpreter for PostScript and PDF
git-who = "formula"  # Git blame for file trees
httpie = "uv"  # User-friendly cURL replacement (command-line HTTP client)
imagemagick = "formula"  # Tools and libraries to manipulate images in many formats
jq = "formula"  # Lightweight and flexible command-line JSON processor
latexindent = "formula"  # Add indentation to LaTeX files
leaf-md = "formula"  # Terminal Markdown previewer with a GUI-like experience
lychee = "formula"  # Fast, async, resource-friendly link checker
mcat = "formula"  # Terminal image, video, directory, and Markdown viewer
mole = "formula"  # Deep clean and optimize your Mac
mq = "formula"  # Jq-like command-line tool for markdown processing
nano = "formula"  # Free (GNU) replacement for the Pico text editor
nnn = "formula"  # Tiny, lightning fast, feature-packed file manager
oxipng = "formula"  # Multithreaded PNG optimizer written in Rust
pandoc = "formula"  # Swiss-army knife of markup format conversion
pastel = "formula"  # Command-line tool to generate, analyze, convert and manipulate colors
pngcrush = "formula"  # Optimizer for PNG files
prek = "uv"  # Pre-commit re-implemented in Rust
pv = "formula"  # Monitor data's progress through a pipe
rumdl = "uv"  # Markdown Linter and Formatter written in Rust
shellcheck-py = "uv"  # Static analysis and lint tool, for (ba)sh scripts
shfmt-py = "uv"  # Autoformat shell script source code
sqlfluff = "uv"  # SQL linter and auto-formatter for Humans
supabase = "formula"  # Postgres development platform
superseedr = "formula"  # BitTorrent Client in your Terminal
swift-format = "formula"  # Formatting technology for Swift source code
swiftlint = "formula"  # Tool to enforce Swift style and conventions
tectonic = "formula"  # Modernized, complete, self-contained TeX/LaTeX engine
telnet = "formula"  # User interface to the TELNET protocol
testdisk = "formula"  # Powerful free data recovery utility
tombi = "uv"  # TOML formatter, linter and language server
tree = "formula"  # Display directories as trees (with optional color/HTML output)
trufflehog = "formula"  # Find and verify credentials
wget = "formula"  # Internet file retriever
xleak = "formula"  # Terminal Excel viewer with an interactive TUI
yamllint = "uv"  # Linter for YAML files
yq = "formula"  # Process YAML, JSON, XML, CSV and properties documents from the CLI
yt-dlp = "uv"  # Feature-rich command-line audio/video downloader

[shell-tools]
autojump = "formula"  # Shell extension to jump to frequently used directories
starship = "formula"  # Cross-shell prompt for astronauts
thefuck = "formula"  # Programmatically correct mistyped console commands
zsh-autosuggestions = "formula"  # Fish-like fast/unobtrusive autosuggestions for zsh
zsh-syntax-highlighting = "formula"  # Fish shell like syntax highlighting for zsh

[python-tools]
ipython = "uv"  # Interactive computing in Python
marimo = "uv"  # Reactive notebook for Python [Jupyter alternative]
mypy = "uv"  # Experimental optional static type checker for Python
"pantsbuild/tap/pants" = "cask"  # Fast, scalable, user-friendly build system for codebases of all sizes
playwright = "uv"  # Framework for web testing and automation
poetry = "uv"  # Python package management tool
pylint = "uv"  # It's not just a linter that annoys you!
ruff = "uv"  # Extremely fast Python linter, written in Rust
ty = "uv"  # Extremely fast Python type checker, written in Rust

[utilities]
6753110395 = "mas"  # Folder Quick Look: Instant Folder & Archive Peek
chromedriver = "cask"  # Automated testing of webapps for Google Chrome
jupyter-notebook-viewer = "cask"  # Utility to render Jupyter notebooks
markdown-preview = "cask"  # Markdown previewer with bundled Quick Look extension
quicklook-video = "cask"  # Thumbnails, static previews, cover art and metadata for video files
syntax-highlight = "cask"  # Quicklook extension for source files

[system-binaries]
bash = "formula"  # Bourne-Again SHell, a UNIX command interpreter
git = "formula"  # Distributed revision control system
grep = "formula"  # GNU grep, egrep and fgrep [command: ggrep]
zsh = "formula"  # UNIX shell (command interpreter)

[languages]
go = "formula"  # Open source programming language to build simple/reliable/efficient software
node = "formula"  # Open-source, cross-platform JavaScript runtime environment
rust = "formula"  # Safe, concurrent, practical language

[fun]
fastfetch = "formula"  # Like neofetch, but much faster because written mostly in C
gitlogue = "formula"  # Cinematic Git commit replay tool
lolcat = "formula"  # Rainbows and unicorns in your console!
lucky-commit = "formula"  # Customize your git commit hashes!
satellite-tracker = "formula"  # Terminal-based real-time satellite tracking and orbit prediction application
scc = "formula"  # Fast and accurate code counter with complexity and COCOMO estimates
